const sumNumbers = (numbers) => {

    // edit this function to return the sum of the input array
    return numbers

}

const countNumbers = (numbers) => {

    // edit this function to return an object that counts the frequency of each number in the array
    return numbers
    
}

module.exports = {
    sumNumbers,
    countNumbers
}